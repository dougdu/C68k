# ---- Build output --------------------------------------------------------
/build/
/out/
/stage1/
/stage2/
/stage3/

# Object / archive / image artifacts
*.o
*.a
*.s
*.elf
*.bin
*.map
*.lst
*.sym

# Target executables (build products)
*.prg
*.PRG
*.68k
*.68K

# Host build of the cross-compiler
*.obj
*.exe
/cc
/c68k

# Emulator / test transcripts
*.tee
*.log
build/*.img
*.nv

# ---- Vendored, hermetic sim test environment ----------------------------
# Populated by tools/bootstrap-simenv.ps1 (renamed c68k-sim68k.exe + boot ROM
# + CP/M / Osiris disk images + cpmtools). ~19 MB vendored from sibling repos;
# re-created on demand, never committed. Per-run scratch lives under /build/.
/simenv/

# ---- Editor / OS ---------------------------------------------------------
.vscode/
*.swp
*~
.DS_Store
Thumbs.db
